apiVersion: v1
kind: Service
metadata:
  name: nginx-rolling-service
spec:
  type: NodePort
  selector:
    app: nginx-rolling-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30090
