nameOverride: simple-nginx

replicaCount: 2

ingress:
  enabled: true
  className: nginx
  path: /nginxa
  pathPattern: /nginxa(/|$)(.*)

content:
  pageTitle: Nginx A
  heading: Hello from nginx A
  message: This page was rendered from the Helm values file for the nginxa release.
