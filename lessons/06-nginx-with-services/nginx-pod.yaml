apiVersion: v1
kind: Pod
metadata:
  name: custom-nginx-service
  labels:
    app: custom-nginx-service
spec:
  containers:
    - name: nginx
      image: nginx:1.25
      ports:
        - containerPort: 80
